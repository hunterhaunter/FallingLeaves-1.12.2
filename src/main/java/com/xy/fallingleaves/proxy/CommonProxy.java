package com.xy.fallingleaves.proxy;

/**
 * Server-side proxy. Falling Leaves is a client-only cosmetic mod,
 * so the server proxy does nothing.
 */
public class CommonProxy {

    public void preInit() {
        // server side — no-op
    }

    public void init() {
        // server side — no-op
    }
}
